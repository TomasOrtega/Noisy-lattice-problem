[project]
name = "noisy-lattice-problem"
version = "0.1.0"
description = "Recover an underlying lattice structure from noisy 2D measurements"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "scipy>=1.18.1",
]

[dependency-groups]
dev = [
    "prek==0.5.3",
]

[tool.uv]
package = false
